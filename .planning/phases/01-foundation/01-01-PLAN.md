---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .gitignore
  - .nvmrc
  - app/package.json
  - infra/package.json
  - scripts/teardown-checklist.sh
  - docs/phase-01/00-overview.md
  - docs/phase-01/01-aws-account-setup/guide.md
  - docs/phase-01/01-aws-account-setup/exercise.md
  - docs/phase-01/01-aws-account-setup/cheatsheet.md
  - docs/phase-01/progress-log.md
  - docs/phase-01/rebuild-log.md
  - docs/phase-01/phase-gate-checklist.md
autonomous: true
requirements:
  - FOUND-06

must_haves:
  truths:
    - "Monorepo skeleton exists with /app, /infra, /docs, /scripts directories and pnpm workspace config"
    - "AWS account setup guide covers IAM user creation (not root), MFA, CLI v2 configuration, and budget alert ($10/month)"
    - "Phase gate checklist lists every verifiable skill from FOUND-01 through FOUND-06"
    - "Progress log and rebuild log templates exist with clear structure for tracking"
  artifacts:
    - path: "package.json"
      provides: "Root package.json for pnpm workspace"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace config listing app, infra, docs, scripts"
    - path: ".gitignore"
      provides: "Ignores node_modules, .env, *.pem, .aws/, .terraform/, IDE files"
    - path: "docs/phase-01/01-aws-account-setup/guide.md"
      provides: "IAM user + MFA + CLI + budget alert conceptual guide"
      min_lines: 80
    - path: "docs/phase-01/01-aws-account-setup/exercise.md"
      provides: "Hands-on steps to create IAM user, configure CLI, set budget"
      min_lines: 60
    - path: "docs/phase-01/phase-gate-checklist.md"
      provides: "Verification checklist for all FOUND-01 through FOUND-06 requirements"
      min_lines: 30
  key_links:
    - from: "docs/phase-01/01-aws-account-setup/guide.md"
      to: "AWS IAM docs"
      via: "External links to official AWS documentation"
    - from: "docs/phase-01/phase-gate-checklist.md"
      to: "All topic exercises"
      via: "References to each exercise for verification"
---

<objective>
Initialize the monorepo skeleton and create the AWS account setup learning materials (IAM, CLI, budget alerts). This is the foundation for all subsequent Phase 1 guides and for the entire project structure.

Purpose: The monorepo structure carries through all 8 phases. AWS account security and budget alerts must be the very first thing configured before any AWS spending begins.
Output: Working pnpm monorepo skeleton, AWS account setup guide/exercise/cheatsheet, phase overview, progress tracking templates, phase gate checklist.
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize monorepo skeleton and project scaffolding</name>
  <files>
    package.json
    pnpm-workspace.yaml
    .gitignore
    .nvmrc
    app/package.json
    infra/package.json
    scripts/teardown-checklist.sh
  </files>
  <action>
Create the monorepo skeleton per the locked decision in CONTEXT.md:

1. **Root package.json**: name "a-journey-to-system-architect", private: true, no dependencies yet.

2. **pnpm-workspace.yaml**: list packages: app, infra, docs, scripts.

3. **Subdirectory placeholders**:
   - `app/package.json` with just `{"name": "app", "private": true}`
   - `infra/package.json` with just `{"name": "infra", "private": true}`
   - Create empty `docs/phase-01/` directory structure matching the research recommendation:
     - `docs/phase-01/01-aws-account-setup/`
     - `docs/phase-01/02-linux-fundamentals/`
     - `docs/phase-01/03-processes-permissions/`
     - `docs/phase-01/04-networking-vpc/`
     - `docs/phase-01/04-networking-vpc/diagrams/`
     - `docs/phase-01/05-security-groups/`
     - `docs/phase-01/06-dns/`
     - `docs/phase-01/07-ssl-tls/`

4. **.gitignore**: Include patterns from the research code examples section (node_modules, .env, *.pem, .aws/, .terraform/, IDE files, OS files, build artifacts).

5. **.nvmrc**: Pin to Node 20 (`20`).

6. **scripts/teardown-checklist.sh**: A bash script that prints a checklist reminder of AWS resources to check and terminate after each practice session. Include: EC2 instances, Elastic IPs, EBS volumes, NAT Gateways, custom VPCs/subnets. Make it executable. This is a reminder script (echo statements), not an automated destroyer.

Run `pnpm install` at root to initialize the workspace after creating files.
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f .gitignore && test -f .nvmrc && test -f app/package.json && test -f infra/package.json && test -f scripts/teardown-checklist.sh && test -d docs/phase-01/01-aws-account-setup && test -d docs/phase-01/07-ssl-tls && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Monorepo skeleton exists with all directories, pnpm workspace initializes cleanly, .gitignore covers all required patterns, teardown script is executable</done>
</task>

<task type="auto">
  <name>Task 2: Create AWS account setup guides and phase tracking templates</name>
  <files>
    docs/phase-01/00-overview.md
    docs/phase-01/01-aws-account-setup/guide.md
    docs/phase-01/01-aws-account-setup/exercise.md
    docs/phase-01/01-aws-account-setup/cheatsheet.md
    docs/phase-01/progress-log.md
    docs/phase-01/rebuild-log.md
    docs/phase-01/phase-gate-checklist.md
  </files>
  <action>
Create 7 markdown files following the locked "concept then build" pattern:

1. **00-overview.md**: Phase 1 overview document. Include:
   - Learning objectives (map to FOUND-01 through FOUND-06)
   - Prerequisites (AWS account, domain name, terminal access)
   - Estimated time (note 1-2 hours/day pace)
   - Topic order: AWS account setup -> Linux fundamentals -> Processes/permissions -> Networking/VPC -> Security groups -> DNS -> SSL/TLS
   - ASCII diagram showing the phase learning path

2. **01-aws-account-setup/guide.md** (FOUND-06): "Concept then build" format.
   - 2-3 paragraphs on WHY: root account dangers, principle of least privilege, why budget alerts matter
   - WHAT: IAM users vs IAM Identity Center (explain both, recommend IAM user for solo learner per research). MFA explanation. AWS CLI v2 overview. AWS Budgets overview.
   - Include links to official AWS IAM best practices docs, AWS Budgets docs
   - Explain that IAM Identity Center with temporary credentials is the production best practice for teams
   - ASCII diagram of IAM hierarchy (root -> IAM users -> policies)

3. **01-aws-account-setup/exercise.md** (FOUND-06): Step-by-step hands-on.
   - Step 1: Sign into root account, enable MFA on root
   - Step 2: Create IAM user with AdministratorAccess, enable MFA
   - Step 3: Create access keys, store in ~/.aws/credentials (warn: NEVER commit)
   - Step 4: Install AWS CLI v2, run `aws configure`, verify with `aws sts get-caller-identity`
   - Step 5: Create $10/month budget alert using AWS console (with CLI alternative from research)
   - Step 6: Verify budget appears in Billing dashboard
   - Each step: exact console navigation path OR CLI command
   - End with "Clean up" section (budget stays, nothing to tear down)
   - End with verification: "Run `aws sts get-caller-identity` -- you should see your IAM user ARN, not root"

4. **01-aws-account-setup/cheatsheet.md**: Quick reference.
   - AWS CLI auth commands (configure, sts get-caller-identity, whoami equivalent)
   - Budget CLI commands (from research code examples)
   - Common IAM CLI commands (list-users, get-user)
   - Credential file locations (~/.aws/credentials, ~/.aws/config)

5. **progress-log.md**: Template with table columns: Date | Topic | What I Did | Time Spent | Screenshots/URLs | Notes. Pre-fill topic rows for all 7 sections. Include instructions at top on how to fill it in.

6. **rebuild-log.md**: Template with columns: Date | Topic | Attempt # | Time Taken | Issues Encountered | Resolved? Include instructions explaining the rebuild challenge concept.

7. **phase-gate-checklist.md**: Complete verification checklist covering ALL requirements:
   - FOUND-01: [ ] Can SSH into EC2, [ ] Can navigate filesystem (ls, cd, pwd, find), [ ] Can view/edit files (cat, less, nano)
   - FOUND-02: [ ] Can list processes (ps, top), [ ] Can manage services (systemctl), [ ] Can set permissions (chmod, chown), [ ] Can manage env vars (export, .bashrc)
   - FOUND-03: [ ] Can explain VPC purpose, [ ] Can diagram public/private subnets, [ ] Can explain security group rules, [ ] Can trace request through route tables
   - FOUND-04: [ ] Can create A record pointing to EC2, [ ] Can create CNAME record, [ ] Can verify DNS with dig/nslookup
   - FOUND-05: [ ] Can install Certbot and get Let's Encrypt certificate, [ ] Can verify HTTPS works (curl -I), [ ] Can explain ACM vs Let's Encrypt tradeoffs
   - FOUND-06: [ ] Have IAM user (not root) with MFA, [ ] AWS CLI works (aws sts get-caller-identity), [ ] Budget alert set at $10/month
   - Final gate: ALL items checked before proceeding to Phase 2

All guides must link to relevant official AWS documentation pages. All materials in English.
  </action>
  <verify>
    <automated>test -f docs/phase-01/00-overview.md && test -f docs/phase-01/01-aws-account-setup/guide.md && test -f docs/phase-01/01-aws-account-setup/exercise.md && test -f docs/phase-01/01-aws-account-setup/cheatsheet.md && test -f docs/phase-01/progress-log.md && test -f docs/phase-01/rebuild-log.md && test -f docs/phase-01/phase-gate-checklist.md && grep -q "FOUND-01" docs/phase-01/phase-gate-checklist.md && grep -q "FOUND-06" docs/phase-01/phase-gate-checklist.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Phase overview describes all 7 topics with learning path. AWS account setup guide explains IAM/MFA/CLI/budgets with links to AWS docs. Exercise has step-by-step instructions. Cheatsheet has quick reference commands. Progress log and rebuild log templates are ready to fill in. Phase gate checklist covers all FOUND-01 through FOUND-06 requirements.</done>
</task>

</tasks>

<verification>
- All monorepo files exist and pnpm workspace is functional
- All docs/phase-01 subdirectories created for subsequent plans
- AWS account setup guide follows "concept then build" pattern
- Phase gate checklist references all 6 FOUND requirements
- No app code created (per locked decision)
</verification>

<success_criteria>
- `pnpm install` runs successfully at project root
- `docs/phase-01/00-overview.md` lists all 7 learning topics in order
- `docs/phase-01/01-aws-account-setup/guide.md` explains IAM, MFA, CLI, budgets with AWS doc links
- `docs/phase-01/01-aws-account-setup/exercise.md` has numbered steps a learner can follow
- `docs/phase-01/phase-gate-checklist.md` has checkbox items for every FOUND requirement
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
